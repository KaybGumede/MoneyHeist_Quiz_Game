#pragma once
#ifndef AVATAR_H
#define AVATAR_H

#include <SFML/Graphics.hpp>
#include <iostream>
using namespace std;

/*class Avatar
{
    public:
        Avatar(string userName);
        //Texture avatarTexture;
        //Sprite avatarSprite;
        Texture getTexture();
        double getScore();
        void addScore(double newScore);
        string getUserName();
        ~Avatar();

    protected:

    private:
        string userName;
        double totalScore;
};*/

#endif // AVATAR_H
